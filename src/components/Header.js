import '../styles/header.css';

const Header = () => {
    return (
        <header>
            <h1>ROCK MEMORY GAME</h1>
            <p>Get points by clicking on an image but don't click on any more than once!</p>
        </header>
    );
}

export default Header